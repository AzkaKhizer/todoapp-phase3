# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Better Auth Configuration
# Use the same Neon database as backend (without asyncpg driver prefix)
DATABASE_URL=postgresql://neondb_owner:password@host-pooler.region.aws.neon.tech/neondb?sslmode=require

# CRITICAL: This secret MUST be EXACTLY the same as backend/.env BETTER_AUTH_SECRET
# Generate with: openssl rand -hex 32
BETTER_AUTH_SECRET=your-secret-key-here-min-32-chars-use-same-in-backend
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000
